spring:
  application:
    name: email-service

  mail:
    host: localhost
    port: 1025
    username:
    password:
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false
  
  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: changeme